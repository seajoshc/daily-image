version: "3.8"

services:
  web:
    build: .
    image: daily-image-web:latest
    command: gunicorn daily_image.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - data:/app
    ports:
      - 8000:8000
    environment:
      - ENVIRONMENT=${ENVIRONMENT}
volumes:
  data:
